#include "GridSpline.h"

void AGridSpline::InitializeSpline(int32 SizeX, int32 SizeY)
{
	//auto * SplineObject = NewObject<USplineComponent>(USplineComponent::StaticClass());
	//SplineObject->ClearSplinePoints();
	//SplineObject->SetClosedLoop(false);
	//int NumberPoint = 0;
	//for (int i = 0; i < SizeY; i++) {
	//	for (int j = 0; j < SizeX; j++) {
	//		SplineObject->AddSplinePoint(FVector(j * 200, i * 200, 0), ESplineCoordinateSpace::Local);
	//		AGridCase * GridCase = NewObject<AGridCase>(AGridCase::StaticClass());
	//		//Spline -> Get
	//	}
	//}


}